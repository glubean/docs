import { AskAgent } from "../../../components/ask-agent";

# Logs, Traces, Metrics

Use these APIs when a pass or fail result alone does not explain enough.

<AskAgent
  prompts={[
    {
      intent: "Explain the run",
      text: "My run is green but the evidence doesn't tell me what happened — add logs, actions, and metrics to «this check» so the report explains itself.",
    },
    {
      intent: "Business metric",
      text: "Record «a business metric, e.g. time-to-first-result» during this test and make it visible in the run report.",
    },
  ]}
/>

## `ctx.log()`

Use structured logs for useful context that should travel with the run.

```typescript
import { test } from "@glubean/sdk";

export const orderTest = test("order-test", async (ctx) => {
  const orderId = "ord_123xyz";

  ctx.log("Starting order processing", {
    orderId,
    items: 4,
    userId: "usr_abc",
  });
});
```

Logs appear in the Result Viewer timeline and are uploaded to Cloud with the run.

## `ctx.metric()`

Use metrics for values you want to chart over time.

```typescript
import { configure, test } from "@glubean/sdk";

const { http } = configure({
  http: { prefixUrl: "{{BASE_URL}}" },
});

export const reportTest = test("report-test", async (ctx) => {
  const start = Date.now();
  const response = await http.get("reports/daily");
  const duration = Date.now() - start;
  ctx.expect(response).toHaveStatus(200);

  const report = await response.json<{ rows: unknown[] }>();

  ctx.metric("report_generation_ms", duration, {
    unit: "ms",
    tags: { reportType: "daily", environment: ctx.vars.get("ENV") },
  });

  ctx.metric("report_row_count", report.rows.length, {
    unit: "count",
  });
});
```

## `ctx.trace()`

Most HTTP traces are created automatically by the Glubean HTTP client returned
from `configure()`. If you use a custom client or raw SDK that Glubean cannot
wrap automatically, emit traces manually.

```typescript
import { test } from "@glubean/sdk";

export const customClientTest = test("custom-client", async (ctx) => {
  const start = Date.now();
  const result = await proprietarySdk.getUser(123);

  ctx.trace({
    method: "RPC",
    url: "grpc://internal.service/getUser",
    status: 200,
    duration: Date.now() - start,
    responseBody: result,
  });
});
```

## `ctx.getMemoryUsage()`

Use this when a heavy test may be leaking memory or processing large payloads.

## Result output

All events (logs, metrics, traces, assertions) are written to `.result.json` files after each run. These files are consumed by:

- **Result Viewer** (VS Code extension) — visual timeline of all events
- **Cloud dashboard** — uploaded via `glubean run --upload`

## Next

- [Use In Cloud](/cloud)
- [VS Code Extension](/extension)
