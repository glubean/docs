import { AskAgent } from "../../components/ask-agent";

# CLI

The `glubean` CLI is the command surface for the local runner. Use it for local
development, CI pipelines, uploads, and team workflows.

In a project, keep the authoring API and runner explicit:

- `@glubean/sdk` — what tests, contracts, workflows, and load plans import.
- `@glubean/runner` — what executes them and emits structured evidence.
- `glubean` — the CLI wrapper you call from your terminal or CI.

<AskAgent
  prompts={[
    {
      intent: "Which command?",
      text: "I want to «run only smoke checks / debug one file / upload evidence». Show me the right glubean command and run it.",
    },
    {
      intent: "Scripts",
      text: "Add package.json scripts for this project's common glubean commands — discover, doctor, local run, CI run, sync, and upload — using the profiles in glubean.yaml.",
    },
  ]}
/>

## Start

```bash
# Initialize a project. The template adds the SDK/runner dependencies it needs.
npx glubean init

# Run CLI commands without installing the CLI globally.
npx glubean <command>
```

## Quick start

```bash
# Scaffold a runnable project. The template adds SDK/runner/local CLI deps.
npx glubean init --no-interactive

# Run the generated local profile through package scripts
npm test

# Inventory assets, then check sync/upload readiness
npm run discover
npm run doctor

# Or call the project CLI directly
npx glubean run --profile local

# Run a single file
npx glubean run tests/users.test.ts
```

## Get help

```bash
npx glubean -h              # All commands
npx glubean run -h          # Options for a specific command
```

## Pages in this section

- [Usage Scenarios](/cli/reference) — common commands organized by workflow
- [Discover & Doctor](/cli/discover) — asset catalog, diagnostics, and Cloud readiness
- [Sync Projections](/cli/sync) — publish source-derived Specifications to Cloud
- [Configuration](/cli/config) — `glubean.yaml` suites, profiles, reporters, upload, and gates
- [Environments & Secrets](/cli/environments) — env files, secrets, switching environments
- [Debugging](/cli/debugging) — narrowing failures and inspecting output
- [Common Recipes](/cli/recipes) — copy-paste patterns for CI, tags, and more
- [Redaction & Privacy](/cli/redaction) — how sensitive data is masked before upload
- [Result Files](/cli/result-files) — .result.json format and artifacts
